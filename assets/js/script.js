// Waits to load DOM
$(function () {

    let input = $("input")

    $("#search-btn").click(function (e) {
        e.preventDefault();
        let searchRequest = input.val()
        console.log(searchRequest)
    })
















































})